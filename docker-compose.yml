version: '3'
services:
  server:
    container_name: artworks_server
    build: ./server
    command: /server
    volumes:
      - ./server:/go/src/github.com/y-yagi/artworks
      - /go/src/github.com/y-yagi/artworks/vendor
    ports:
      - "3001:3001"
    depends_on:
      - db
    environment:
      DATABASE_URL: postgres://artworks:artworks@db/artworks?sslmode=disable
  client:
    container_name: artworks_client
    build: ./client
    command: npm start
    volumes:
      - ./client:/usr/src/app
      - /usr/src/app/node_modules
    ports:
      - "3000:3000"
  db:
    container_name: artworks_db
    build: ./db
    ports:
      - "5434:5432"
    volumes:
      - postgresql:/var/lib/postgresql/data

volumes:
  postgresql:
    driver: local
  client:
    driver: local
  server:
    driver: local
